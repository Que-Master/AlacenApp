<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet para POST hacia el ESP -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Permisos de WiFi -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>

    <!-- Requerido para escanear redes y requestNetwork en Android 10+ -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Android 13+ -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"/>

    <application
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.MyApplication">

        <activity android:name=".view.WifiSetupActivity" android:exported="false"/>
        <activity android:name=".view.DetallesActivity" android:exported="false"/>
        <activity android:name=".view.EditProductActivity" android:exported="false"/>
        <activity android:name=".view.AddProductActivity" android:exported="false"/>
        <activity android:name=".view.ComprasActivity" android:exported="false"/>
        <activity android:name=".view.AlacenaActivity" android:exported="false"/>

        <activity android:name=".view.MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>

</manifest>
